# 共通ルール

日本語で応答する。

## 実装と委譲

実装は自分で書く。サブエージェントには投げない。
仕様も文脈も自分が持っていて、渡し直すと落ちる。

委譲するのは2つの場合だけ。出力が大量で自分の文脈を埋めてしまうときと、
別のモデルに見せる価値があるとき。

- テストの作成・実行 → `test-runner`
- 広範囲の調査 → `Explore`
- プランの批評 → `plan-reviewer`
- ドキュメントの記録 → `doc-writer`
- 差分のレビュー → `/code-review`（組み込み）
- アプリを動かしての確認 → `/run`（組み込み）

数回のツール呼び出しで終わる作業は委譲しない。
自分の仕事を確かめるためのサブエージェントも使わない。

## 作業の進め方

依頼された範囲を実装する。頼まれていないリファクタや抽象化は足さない。
仕様の判断に迷ったら妥当な方を選んで進め、選んだことを報告に含める。
どちらを選んでも成果物が変わる場合だけ手を止めて聞く。

何を作るかが固まっていない大きめの依頼は、`skill: spec` で先に詰める。
diff を1文で説明できる依頼には使わない。

テストが落ちているなら落ちていると出力とともに報告する。
飛ばした手順があるなら飛ばしたと書く。

## 応答の書き方

結論から書く。最初の1文で「何が起きたか」「何が分かったか」に答える。
根拠と詳細はその後。単純な質問には単純に答える。見出しや表で膨らませない。

## ドキュメント

`~/dev` 配下のプロジェクトのドキュメントは `~/dev/doc-storage/<プロジェクト名>/`
に置く。リポジトリ内には置かない。詳細は `skill: doc-storage`。

リポジトリ内でドキュメントを管理する慣習のプロジェクトは対象外。

## 検証

ビルド・テスト・lint のコマンドは各プロジェクトの CLAUDE.md か
`.claude/skills/run-*/` にある。そこに書かれたコマンドで検証する。
どちらにも無ければ `/run-skill-generator` で記録させる。
