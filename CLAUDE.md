# 共通ルール

日本語で応答する。

## モデル運用

モデルはフェーズで切り替える。タスクの途中で相談相手を足すのではなく、
セッションごと切り替える。

- 設計・仕様・研究結果の解釈 → 上位モデル（`opus` / `fable`）のセッション
- 実装・実験の実行・記録 → `sonnet` のセッション

切替点は `spec` の末尾。仕様を書き出したら `/model sonnet` → `/clear` → `/feature <パス>`。

## 実装と委譲

実装は自分で書く。サブエージェントには投げない。
仕様も文脈も自分が持っていて、渡し直すと落ちる。

委譲するのは2つの場合だけ。出力が大量で自分の文脈を埋めてしまうときと、
別のモデルに見せる価値があるとき。

- テストの作成・実行 → `test-runner`
- 広範囲の調査 → `Explore`
- プランの批評 → `plan-reviewer`
- 行き詰まったときの前提の点検 → `advisor`
- ドキュメントの記録 → `doc-writer`
- 差分のレビュー → `/code-review`（組み込み）
- アプリを動かしての確認 → `/run`（組み込み）

数回のツール呼び出しで終わる作業は委譲しない。
自分の仕事を確かめるためのサブエージェントも使わない。

## 作業の進め方

**ゴールが明確でなければ実装を始めない。**
まず完了条件を「検証可能な1文」で書けるか試す。書けたかどうかで進み方が決まる。

- 書けて、diff も1文で説明できる規模 → そのまま実装する
- 書けるが、触るファイルが複数で実装方針が1本に決まらない → `skill: feature`
- 書けない → 書けるところまで聞く。それでも固まらない大きめの依頼は `skill: spec`

**この判断は自分でする。** ユーザーがスキル名やスラッシュコマンドを打つのを待たない。
ユーザーは作りたいものを言うだけでよく、どの手順に入るかはこちらの責任。

依頼された範囲を実装する。頼まれていないリファクタや抽象化は足さない。
仕様の判断に迷ったら妥当な方を選んで進め、選んだことを報告に含める。
どちらを選んでも成果物が変わる場合だけ手を止めて聞く。

テストが落ちているなら落ちていると報告する。出力を添える。
飛ばした手順があるなら飛ばしたと書く。

## 自律の範囲

完了条件が検証可能な1文で書けているなら、そのまま自走してよい。
分岐は観測できる事象だけで決める。「難しそうか」「自信があるか」では決めない。
**レビューも停止も、条件に当たったら自分から起動する。** 指示を待たない。

| 事象 | すること |
|---|---|
| プロジェクトの CLAUDE.md が「不変条件」に挙げたファイルに触る | 実装前に `plan-reviewer` |
| 1回の試行が高い（実機・数十分かかる） | 自律に入る前に `plan-reviewer` |
| 同じ完了条件に異なる手で2回失敗した | `advisor` に「どの前提が間違っているか」を問う |
| `advisor` の後も解消しない | 停止して人に返す |
| 人の判断・実機の事実が必要になった | 到達点を記録して停止 |
| コミットする前 | `/code-review` |

「異なる手で2回」は、同じ修正のリトライを1回に数える。
ビルドエラーを3回直したのは1アプローチの反復であって、2回失敗ではない。

## 応答の書き方

結論から書く。最初の1文で「何が起きたか」「何が分かったか」に答える。
根拠と詳細はその後。単純な質問には単純に答える。見出しや表で膨らませない。

## ドキュメント

プロジェクトのドキュメントはリポジトリ内に置かず、doc-storage に集約する。
配置ルートと規約は `skill: doc-storage`。

リポジトリ内でドキュメントを管理する慣習のプロジェクトは対象外。

## 検証

ビルド・テスト・lint のコマンドは各プロジェクトの CLAUDE.md か
`.claude/skills/run-*/` にある。そこに書かれたコマンドで検証する。
どちらにも無ければ `/run-skill-generator` で記録させる。

## ハーネス

このハーネス（`~/.claude` のルール・エージェント・スキル）は作業中に書き換えない。
気付いたことは worklog の「ハーネスへの示唆」欄に書き、変更は `skill: harness-review` で行う。
